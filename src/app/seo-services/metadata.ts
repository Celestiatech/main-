import type { Metadata } from "next";
import { generateMetadata as genMeta } from "@/lib/metadata";

export const metadata: Metadata = genMeta({
  title: "SEO Services — Technical, Local & AI Search SEO",
  description: "Technical SEO, content structure and internal linking that moves rankings. Core Web Vitals, schema markup and answer-engine optimisation from a development team.",
  path: "/seo-services",
});
