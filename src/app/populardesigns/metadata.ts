import type { Metadata } from "next";
import { generateMetadata as genMeta } from "@/lib/metadata";

export const metadata: Metadata = genMeta({
  title: "Premium Website Design Templates & Themes",
  description: "Browse premium website design directions before we build yours. Explore layouts, themes and visual styles for business sites, portfolios and online stores.",
  path: "/populardesigns",
});
