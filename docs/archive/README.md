# Archived docs

These guides describe the admin dashboard (`/admin`, `/api/admin`, password
login, SQLite message store). That feature set was removed from the project —
the site is now stateless and contact/career submissions are delivered by email
instead. The documents are kept for reference only; nothing here reflects the
current codebase.
